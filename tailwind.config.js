// eslint-disable-next-line
const colors = require('tailwindcss/colors');

module.exports = {
  content: [],
  theme: {
    colors: {
      white: colors.white,
      teal: colors.teal,
      gray: '#1a202c',
      neutral: colors.neutral,
      red: colors.red,
    },
  },
};
